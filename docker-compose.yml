version: '3.7'

services:

  app:
    container_name: ekubo
    build:
      context: .
      dockerfile: .
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - 5000:5000
    environment:
      - CHOKIDAR_USEPOLLING=true